using UnityEngine;

// class declaration
public class HelloWorldScript : MonoBehaviour              // class named after the script's file
{
    // all code goes inbetween these { }

    // Use this for initialization
    void Start()           // method/function called Start
    {
        print("Hello World");             // runs the world "Hello World" when the scene first starts
    }

    // Update is called once per frame
    void Update()          // method/function called Update
    {
        // any code inside here runs as fast as possible
    }
}