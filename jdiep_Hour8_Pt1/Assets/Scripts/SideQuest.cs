using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SideQuest : MonoBehaviour
{
    /*
    int TakeDamage(int damageAmount)
    {
        int health = 100;
        return health - damageAmount;
    }
     * The method's name is: TakeDamage.
     * The variable type that the method returns is: int.
     * The method has one parameter and it is called: damageAmount.
     * The code that is in the method's block is: int health = 100; and return health - damageAmount;
     */

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
